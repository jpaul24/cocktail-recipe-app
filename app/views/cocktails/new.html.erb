<div class="new-cocktail-container">
  <div class="page-items-new">
    <div class="new-form">
    <%= simple_form_for(@cocktail) do |f| %>
           <%= f.input :name %>
           <%= f.input :description %>
           <%= f.input :photo, as: :file %>
           <%= f.button :submit, "Add New Cocktail", class: 'text-center btn btn-primary'%>
          <% end %>
    </div>
  </div>

</div>


